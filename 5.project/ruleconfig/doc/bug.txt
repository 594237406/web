1.读取后的触发因子编排展示预览区 （完成）
2.报错定位bug ，正则判断bug （完成）
3.国际化没提交上去 （完成）
4.单击树节点不改变颜色（完成）
5.保存读取出错（完成）

6.按钮改成与appengine相同，按钮鼠标悬浮提示 ，操作按钮透明度调高 （完成）
7.日期控件没有做国际化 （无法修改，与浏览器语言环境有关）
8.去掉谷歌翻译，（在设置-高级设置-语言）
9.左右括号分颜色配对（不做）
10.预览区在input标签失去焦点刷新（完成）
11.去掉SRC路径：址栏输入about:flags进入项Compact Navigation点启用重启chrome标签右击项hide the toolbar",选择
12.删除按钮换成X （完成）

13.双击触发因子直接按顺序添加到编排区（完成）
14.URL的编辑方式，让人不太容易理解，建议改为ListNav（完成）
15.原子事件触发规则页面弹出后，窗口title是rulelayout，也要修改下吧，比如rule settings之类的？（与后台融合国际化）
16.ListNav在点开之后没有反选（完成）
17.webset没有值，非select不能添加值（完成）
18.左侧树的样式需要调整为AppEngine的树样式（完成）
19.宽高自适应，边框和背景图片采用背景图（完成）
20.拖进来的编辑框长度建议再调长点，内容与边框的间距也适当再调大点（完成）
21.日期选择框中点击了叉号之后，变成了中文的年月日，而预览或者保存的时候没有校验（已有）
22.弹出框open样式，按钮样式与Appengine一致（不做，做完无法调整窗口大小）
23.预览增加校验，校验改成非alert，改成窗口悬浮（完成）
24.保存、预览校验（预览不校验，否则每次失去焦点都会校验）
25.针对单选或者多选，前面的运算符肯定是等于或者in，因此运算符可以隐藏掉，把选择框的宽度调大一些，
占据运算符的区域（完成）
26.针对单选或者多选，选择出列表项之后，不应该显示value，而应该显示name，多选值以逗号分隔，不能显示选择了几项（完成）
27.拖拽触发因子在除了编排区松手时没有改变节点颜色（完成）
28.URL过多长度问题（完成）