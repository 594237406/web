 //后台中
  System.out.println(Test.class.getResource("/"));            // 文件编译根路径
  System.out.println(Test.class.getResource(""));             // 文件编译绝对路径
  System.out.println(System.getProperty("user.dir"));            // 项目绝对路径
  System.out.println(System.getProperty("user.dir").
    substring(System.getProperty("user.dir").lastIndexOf("\\")));       // 项目相对路径
  System.out.println(new File("/").getAbsolutePath());           // 获得盘符
  //Web中
  //request.getContextPath()                  // 项目相对路径(上下文路径)
  //application.getRealPath("/");                 // 项目在服务器绝对路径
  //request.getSession().getServletContext().getRealPath("/")          // 项目在服务器绝对路径
  //request.getScheme()+"://"+request.getServerName()+":"+request.getServerPort()+path+"/"; //协议名+IP+端口号+上下文路径
  //request.getRequestURI()                  // 页面相对路径(带上下文路径)
  //request.getServletPath()                  // 页面相对路径(不带上下文路径)
  //application.getRealPath(request.getRequestURI())               // 页面在服务器绝对路径
  