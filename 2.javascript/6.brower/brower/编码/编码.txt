1.
<meta charste="utf-8"> 只是告诉浏览器要用utf-8来解释，而文档的编码，是在你保存时的选择决定的。
如果保存ANSI 然后用utf-8解释，肯定是乱码。记事本的话，默认保存的文件格式是ANSI。所以在保存的时候要修改为uif-8。

2.
浏览器更改编码相当于修改解释方式，即meta。比如文件格式为ANSI，meta设置为utf-8，则为乱码。
浏览器编码需改为gbk。